# Provenance and review status

- Visual system: MVN Climate Atlas, approved from the three calibration heroes in `outputs/moodboards/mvn-calibration-v1/`.
- Base scenes: generated with the native ImageGen worker path from prompts in `jobs.jsonl`.
- Technical corrections: source-preserving edits defined in `revisions/jobs.jsonl`.
- Exact engineering overlays: deterministic composition in `compose_heroes.py`.
- Published candidates: optimized WebP files in `web/public/img/blog/v2/`.
- Review surface: `review-board.html`, `contact-sheet.png` and the normalized moodboard payload.

No generated text, product logos, brand marks, prices, claims or UI are used. The current pack is review-ready and connected in MDX; it has not been declared technically immutable and can be revised from installer feedback without regenerating the base scenes.
