<section class="home-trust" aria-labelledby="home-trust-title">
    <div class="container">
        <div class="home-trust__heading">
            <p>До и после монтажа</p>
            <h2 id="home-trust-title">Понятный процесс без технических сюрпризов</h2>
        </div>

        <div class="home-trust__grid">
            <article>
                <span class="material-icons-round" aria-hidden="true">fact_check</span>
                <h3>Сначала уточняем объект</h3>
                <p>Смотрим помещение, размещение блоков, трассу, дренаж и доступ к фасаду.</p>
            </article>
            <article>
                <span class="material-icons-round" aria-hidden="true">receipt_long</span>
                <h3>Согласуем состав и стоимость</h3>
                <p>До начала работ объясняем схему установки, материалы и возможные доплаты.</p>
            </article>
            <article>
                <span class="material-icons-round" aria-hidden="true">cleaning_services</span>
                <h3>Остаёмся на связи после запуска</h3>
                <p>Можно обратиться за плановой чисткой, диагностикой или ремонтом системы.</p>
            </article>
        </div>
    </div>
</section>

<style>
    .home-trust {
        padding: 5rem 0;
    }

    .home-trust__heading {
        max-width: 720px;
        margin-bottom: 2rem;
    }

    .home-trust__heading p {
        color: var(--primary);
        font-size: 0.78rem;
        font-weight: 800;
        text-transform: uppercase;
    }

    .home-trust h2 {
        margin-top: 0.35rem;
        font-size: clamp(1.9rem, 4vw, 2.8rem);
    }

    .home-trust__grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        border-top: 1px solid var(--border);
    }

    .home-trust article {
        padding: 1.75rem 1.75rem 1rem 0;
        border-right: 1px solid var(--border);
    }

    .home-trust article + article {
        padding-left: 1.75rem;
    }

    .home-trust article:last-child {
        border-right: 0;
    }

    .home-trust .material-icons-round {
        color: var(--primary);
        font-size: 30px;
    }

    .home-trust h3 {
        margin-top: 0.9rem;
        font-size: 1.2rem;
    }

    .home-trust article p {
        margin-top: 0.6rem;
        color: var(--text-muted);
        font-size: 0.92rem;
        line-height: 1.6;
    }

    @media (max-width: 720px) {
        .home-trust {
            padding: 4rem 0;
        }

        .home-trust__grid {
            grid-template-columns: 1fr;
        }

        .home-trust article,
        .home-trust article + article {
            padding: 1.4rem 0;
            border-right: 0;
            border-bottom: 1px solid var(--border);
        }
    }
</style>
