# Include inside the `server_name api.mvn.by` HTTPS server block before
# the catch-all `location /` proxy.
location /media/ {
    alias /opt/air-api/media/;
    try_files $uri =404;

    access_log off;
    expires 30d;
    add_header Cache-Control "public, max-age=2592000";
}
